@{
    ViewBag.Title = "Profile Updated";
}

<div class="container mt-5">
    <div class="row">
        <div class="col-md-8 offset-md-2">
            <div class="card">
                <div class="card-header">
                    <h2 class="card-title text-center">Profile Updated Successfully</h2>
                </div>
                <div class="card-body text-center">
                    <p>Your profile has been updated successfully.</p>
                    <div class="d-flex justify-content-center">
                        <a href="@Url.Action("Index", "Home")" class="btn btn-primary me-2">Go to Home</a>
                        <a href="@Url.Action("Edit", "User")" class="btn btn-secondary">Back to Edit Profile</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
